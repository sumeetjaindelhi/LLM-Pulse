# package-lock.json is intentionally tracked: it pins the dependency tree so
# `npm ci` is reproducible and `npm audit fix` results persist in the repo.
node_modules/
dist/
*.tsbuildinfo
.DS_Store
Thumbs.db
.env
*.log
ARCHITECTURE.md
CLAUDE.md
*.md
!README.md
*.yml
